#include "Logger.h"
#include <fstream>

void Logger::write(std::string message)
{
	/*
	if(message.size()==1 && message[0]>='a' && message[0]<='z') return;
	std::ofstream ofs( "log.txt", std::ios::app);
	ofs << message;
	*/
}

void Logger::out()
{
}
